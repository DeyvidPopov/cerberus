// WebSocket handlers for real-time telemetry and continuous authentication
// (PROJECT.md §2; Phase 7). Telemetry is bound to an authenticated session and
// re-scored in-session; a risk spike locks the vault (fail closed).
//
// Empty in Phase 0.
export {};
